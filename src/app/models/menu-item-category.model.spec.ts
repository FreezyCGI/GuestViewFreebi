import { MenuItemCategory } from './menu-item-category.model';

describe('MenuItemCategory', () => {
  it('should create an instance', () => {
    expect(new MenuItemCategory()).toBeTruthy();
  });
});
